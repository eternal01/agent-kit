# 完成声明证据

任何 Agent 在声明代码、配置、文档或计划“已完成、已修复、可合并或检查通过”前，必须：

1. 将声明拆成可验证条件，并为每项给出直接证据。
2. 运行当前环境中相关且最新的检查，记录命令、退出状态和关键结果。
3. 对文档检查链接、命令、示例、占位符和需求覆盖；对 Skill 变更检查触发契约和静态验证。
4. 明确列出未验证项及原因；不得将有限检查外推为完整通过。
5. 仅在证据支持的范围内下结论。

测试、构建、原问题复现、静态检查、diff 与逐条需求核对各自只能证明其覆盖范围。