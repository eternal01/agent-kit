# 测试与变更原则

## 测试层级
- **单元测试**：纯业务规则、边界值和错误分支。
- **契约/集成测试**：数据库、队列、外部 API、序列化及模块接口。
- **端到端测试**：少量关键用户链路，不重复覆盖所有组合。

## 行为变更循环
1. 写一个能解释目标行为的测试并确认它因预期原因失败。
2. 写使其通过的最少代码。
3. 运行相关测试，确认没有回归。
4. 在测试保护下整理重复和命名。

测试观察公开行为，避免把实现细节和大量 mock 固化。修复缺陷时，回归测试应在撤销修复后失败、恢复修复后通过。遵循仓库已有命令和测试风格。
