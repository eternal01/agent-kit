# 技术文档结构模板

## 设计说明
背景与范围 → 目标/非目标 → 约束 → 方案与接口 → 异常/安全 → 验证 → 风险。

## 实施/迁移手册
适用范围 → 前置检查 → 分步操作 → 每步验证 → 监控 → 回滚 → 升级路径。

## 运维排障手册
症状 → 影响判断 → 诊断命令 → 决策分支 → 缓解/恢复 → 验证 → 复盘信息。

## API 文档
用途 → 鉴权 → 请求 → 响应 → 错误 → 幂等/限流/分页 → 示例 → 版本兼容。

## README
用途 → 快速开始 → 配置 → 常用命令 → 测试/构建 → 故障排查 → 贡献与维护。

只选择一种主结构；跨文档内容用链接引用，不复制维护。
